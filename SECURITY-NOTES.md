# ملاحظات أمان — Lucca POS

> نُفّذت إعادة كتابة تاريخ الريبو (2026-09-13): أُزيلت القيمة الاحتياطية الافتراضية لمفتاح API
> المشترك من كل كائنات التاريخ (كود + رسائل commits + tags) واستُبدلت بـ `[REDACTED]`.
> الريبو **Public** — تعامل مع أي خاملة منشورة سابقاً كأنها مكشوفة بالكامل.

## 1. سجل التدوير الفعلي (2026-09-13)

| العنصر | الحالة |
|---|---|
| `DEVICE_API_KEY` (backend/.env) | ✅ دُوّرت بقيمة جديدة عشوائية 256-bit — القديم يرجع 401 |
| `API_KEY` (backend/.env) | ✅ دُوّرت كذلك — القديم `[REDACTED]` يرجع 401 |
| كلمة مرور أدمن الـ SQLite | ✅ دُوّرت مباشرة في DB (hash pbkdf2 جديد) + `mustChangePassword=1` يُجبِر كلمة شخصية عند أول دخول |
| كلمة مرور أدمن الـ Supabase | ✅ دُوّرت عبر REST (كانت plaintext `123456` مقروءة للـ anon) — القديم الآن يرجع 0 صف |
| `mustChangePassword` | ✅ مفعّل — أي عملية مميّزة ترجع 403 «Password change required» حتى يتغيّر |
| نسخ احتياطية قبل التدوير | `backend/.env` + ملفات `lucca.db*` محفوظة خارج الريبو للاسترجاع |

> **خطوة يدوية لكل جهاز كاشير**: سجّل الدخول كأدمن من إعدادات الجهاز وأعد جلب مفتاح الجهاز
> (يجري تلقائياً عبر `/api/auth/provision-device` بعد تسجيل الدخول) — `luccaApiKey` في المتصفح
> يظل بالقديم حتى يُعاد إدخاله.

## 1b. خلل منشور مكتشف أثناء التدوير

- **مُصلَح**: `backend/src/db.ts` — `getCurrentUser()` يستعلم عمود `active` غير الموجود في الـ DB
  الحيّة → `GET /api/auth/me` كان يعيد 401 دائماً. أُضيفت هجرة `ALTER TABLE users ADD COLUMN active`
  (بنمط try/catch ذاته)؛ أُعيد البناء والتشغيل وتحقّق `/api/auth/me` → 200.

## 1c. Supabase (RLS) — نتيجة فحص 2026-09-13

- المفتاح المضمّن في الكود = **ANON key** (JWT `role:"anon"`) — عام بتصميم، وليس `service_role`.
- **لكن**: RLS مفعّل على الجداول الـ 25، ويوجد سياسة `USING (true) WITH CHECK (true)` على كل واحد
  → الوصول الفعلي **مفتوح بالكامل للـ anon key** (قراءة وكتابة).
- فحص REST تجريبي: كل الجداول أعادت 200 للـ anon، وجدول `users` يُرجع عمود `password` فعلاً.
- **نُفّذ فعلياً (لا داعي ليدوي)**: أدمن الجدول `users` في Supabase كان plaintext `123456` (من seed)
  مقروء للـ anon → دُوّرت كلمة المرور عبر UPDATE مباشر (نفس الـ anon key بسبب فتح RLS)
  وتحقّق: `123456` → 0 صف، الجديدة → 1 صف. القيمة الجديدة عند المشغّل فقط.
- **يتبقّى يدوياً (من لوحة Supabase — لا يُصلح من الكود)**:
  1. نفّذ `deploy/supabase-rls-fix.sql` (حذف `USING (true)` + FORCE RLS + قراءة عامة للقائمة فقط).
     **انتبه**: التطبيق يستخدم أنظمة internal login، كل عملائه anon → إغلاق RLS سيفصل المتصفح عن
     المخازن الحساسة (orders/users/payments...) حتى تُهاجر لـ Supabase Auth أو وسيط service_role
     (النمط في نهاية سكربت الإصلاح). الجزء 3أ يبقي القائمة منشورة للعموم كما ينبغي.
- لا داعي لدوران الـ anon key (عام بتصميم) — الإصلاح في RLS فقط.

## 2. قواعد لمنع تكرار التسريب

- لا تُثلج أبداً: `.env`، نسخ احتياطية لقاعدة البيانات، سجلات (`*.log`)، ملفات `*.db`.
  (مستثناة حالياً في `.gitignore` — لا تزلها.)
- **افحص قبل كل push**: 

| الأداة | التشغيل |
|---|---|
| عمل Gitleaks | تلقائي — `.github/workflows/secret-scan.yml` على كل push/PR (يكافح فشل البناء عند وجود سر) |
| مسح محلي بخفة | `node tools/secret-scan.cjs` — HIGH تُخرج 1؛ يفحص كود المتعين فقط، يتجاهل >500KB |

- **معروف ومقصود عمداً**: `admin/123456` بوتستراب التثبيت الجديد (يجبر التغيير أول دخول عبر
  `mustChangePassword`). الماسح يُبلغ عنه HIGH عمداً (5 ملفات) — لا تنزله من القائمة؛
  التثبيتات الحية دُوّرت فعلاً (2026-09-13) لكن bootstrap الجديد سيظل يُنشئ 123456 مؤقتاً.
- **لا fallback حرفية** لمفاتيح في الكود: القراءة من البيئة فقط (`process.env.*`)، وإن غاب المتغير
  فافشل صراحةً بدل التشغيل بمفتاح معروف.
- قبل أي `git push`: امسح للـ keys عبر أداة مثل Gitleaks أو اكتشف الخام قِبل النشر.
- عند كشف سر: **غيّره فوراً** ثم نظّف التاريخ (filter-repo / filter-branch) ثم force-push.

## 3. تحذير للفرق

بعد إعادة الكتابة: **أي نسخة محلية أخرى يجب إعادة استنساخها (clone) من جديد** —
لا `pull`؛ تواريخ الـ commits تغيّرت بالكامل (جميع الـ hashes).