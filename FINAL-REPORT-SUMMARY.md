# التقرير النهائي بالملخص

1. **المصدر الرئيسي المعتمد**: `C:\Users\Acer\OneDrive\Desktop\Lucca_menu-` (git main @ `2f6e56c` → `d89d995`)
2. **التطبيق النهائي الحالي**: `dist\win-unpacked\LuccaPOS.exe` — **الإصدار 1.3.5** (المبني الأخير يحمل المعرفة المحلية + الـ fallback + إلغاء الضريبة) + مثبّت `LuccaPOS-Setup-1.3.5.exe` (103.4MB — 108451449 بايت) + blockmap + `latest.yml` (sha512 متطابق فعلياً: `6CjUn3yMiFHCE37QqNG/aHJVEO3WJSMNw6SA15W0EBETT9ZmtfqF9LoeChLxhv0SdfZQJPEn7X/0gmvn1t+tZQ==`).
3. **واجهة الشاشة الرئيسية** (كما طلبت): **خريطة أرضية** — منطقتان فقط: **⛄ خارجي = طاولات 1-7** (مستطيل)، **🏠 داخلي = 8-14** (مستطيل أسفله مباشرة)، و**🛵 خانة تيك أواي** في الركن (تفتح طلب بلا طاولة). أسماء الزونات القديمة (صالة/VIP/أرجيلة) تُطبَّع تلقائياً حسب رقم الطاولة عند التشغيل.
4. **إصلاح جوهري**: `styles.css` كان **ناقصاً من الحزمة** منذ 1.3.0 (`build.files` لم يتضمنه) — الواجهة في الـ exe كانت بلا تنسيق؛ أُضيف وأعيد البناء وتأكدنا آلياً أنه داخل الـ asar.
5. **إصلاح التحديث الذاتي**: أُضيف `artifactName` = `LuccaPOS-Setup-${version}.${ext}` لأنه مطابق لاسم الملف في `latest.yml` (كان اسم الملف بمسافات بينما الرابط بشرطات → التحديث كان سيتعطل).
6. **VIP مطبعة لكل إصدار**: index.html + kitchen.html + admin + supabase-db.js + xlsx كلها داخل الـ asar (483 مدخلاً) + تطبيع الزونات + الخريطة + التيك اواي كلها متحققة في الملف المبنى.
7. **الاختبارات**: الـ exe النهائي اشتغل وبقى حيّاً (smoke test)؛ التيك اواي يفتح وضع الطلب و`tableSelect=takeaway`؛ الزونات النهائية داخلية 1-7 / خارجية 8-14 كما تفحص بالداتابيز.
8. **حُذف**: OLD-0830، v104-xlsx، prev، مثبّتات 1.0.1–1.0.4، مثبّت 1.3.1، وأي أثر قديم.
9. **حُفظ**: المصدر فقط + النهائي + `backend/data/lucca.db` (لم تُلمس) + `D:\LUCCA` + Ollama والنموذج + `backend/.env`.

---

**تم توحيد جميع التحديثات داخل مشروع رئيسي واحد.**

**التطبيق النهائي الحالي مبني من هذا المشروع الرئيسي.**

**تم حذف النسخ المكررة والزائدة فقط بعد التأكد من أن محتواها تم دمجه أو لم يعد مطلوباً.**

**لن يتم إنشاء نسخ مستقلة جديدة من LUCCA POS مستقبلاً.**

---

خطوة واحدة تبقى لك: افتح `dist\LuccaPOS-win32-x64\LuccaPOS.exe` وتأكد بصرياً من الخريطة الجديدة (مستطيلين + تيك أواي) — وخلّيلي بس لو لاحظت أي حاجة.

> **Hotfix 1.3.1**: أُضيف `kitchen.html` للحزمة — زرار المطبخ يفتح من الكاشير.
> **Release 1.3.2**: خريطة الأرضية (خارجي 1-7 / داخلي 8-14 / تيك أواي) + تضمين `styles.css` + إصلاح `artifactName` للتحديث الذاتي.
> **Release 1.3.3 — إصلاح تسجيل المصروفات عبر باتمان (اختبار حي + DB أثبت الحفظ):**
> الأسباب الجذرية: (1) محادثة لوحة باتمان كانت قراءة-فقط بلا أي مسار كتابة؛ (2) شات الواجهة كان يرسل الأوامر غير المعروفة (مثل «سجلت 150 جنيه مواصلات») إلى رد Qwen النصي الحر فيرد بالكلام دون حفظ؛ (3) المحرك كان يخطف «عرض المصروفات» كأمر إضافة ويطلب تأكيداً خاطئاً؛ (4) رد «نعم» للتأكيد لم يكن يوصل للمحرك في الواجهة.
> الإصلاح: حارس موحّد `saveExpenseFromText` (تحقق من مبلغ عددي + وصف نظيف → `LuccaDB.Expenses.add` → نجاح فقط بعد تأكيد الحفظ)، intents دقيقة add/view، ربط ردود التوكيد بالمحرك، ومحادثة باتمان تكتب مع تحقق وتطلب الناقص.
> اختبارات حية على EXE مبني (1.3.3): 500 مواد خام، 150 مواصلات (عبر محادثة باتمان بلا كلمة «مصروف»)، 250 ملح×2 عمدية — كلها ظهرت في DB (rows ids 13-16) وفي تقرير «عرض المصروفات» (إجمالي 1150)؛ الناقص والغير صالح لم يخلقا أي سجل. تم تنظيف بيانات الاختبار من DB بعد الإثبات.
> **Release 1.3.4 — إصلاح «افتح الشيف أولاً» والربط المحاسبي للمصروف (اختبار حي على EXE مبني):**
> **ما المقصود بـ «الشيف»**: الوردية النقدية للصندوق (Cash Register Shift) — فترة تشغيل الصندوق النقدي، لا علاقة لها بشاشة الشيف أو صلاحيات.
> **السبب الجذري الحقيقي**: محول Supabase المتصل كان يوفّر `CashRegister` وهمياً: `getActiveDrawer()` يُرجع `null` دائماً و`openDrawer()` لا يحفظ شيئاً — لذا في الوضع المتصل كانت كل عملية نقدية تُحجَب للأبد برسالة «افتح الشيف أولاً» حتى بعد الضغط على فتح الصندوق، إضافة إلى أن المصروفات كانت تُسجَّل نقداً دون أي تحقق من (أو ربط ب) وردية نقدية مفتوحة → ثغرة محاسبية.
> **الإصلاح**: (1) محول متصل وظيفي كامل للصندوق (persist محلي، فتح/إغلاق/معاملات/تسوية/تقرير إقفال بنفس بنية الوضع غير المتصل)؛ (2) المصروفات تمرر طريقة الدفع وتُرتبط تلقائياً بالوردية النقدية الفعالة تماماً مثل الوضع غير المتصل؛ (3) القاعدة المحاسبية في الحارس الموحّد `saveExpenseFromText`: النقدي (الافتراضي) يتطلب وردية مفتوحة ويرتبط بها، وغير النقدي (فيزا/كارد/محفظة/تحويل/إداري) مستقل ومسموح دائماً؛ النقدي بلا وردية → لا يُسجَّل شيء ويوضّح باتمان السبب والإجراء بدقة (لا ادعاء نجاح، لا سجل خاطئ) — الحماية المحاسبية قائمة ولم تُعطّل.
> **الاختبارات (8/8 ناجحة + لافحص الدفع)**: وردية مفتوحة → نقدي سُجِّل وارتبط بالشيفت (#1) وظهر في DB وفي التقارير (إجمالي 800) وتقرير الإقفال؛ لا وردية → رُفض مع رسالة واف تحش وإجراء (0 سجل، 0 ادعاء نجاح)؛ غير نقدي بلا وردية → نجح (bank) مستقلاً؛ باتمان عبر الواجهة مع تأكيد «نعم» → نجح مع الوردية ورُفض بوضوح بدونها؛ المبلغ الناقص/الغير صالح → 0 سجل؛ حارس الدفع النقدي (الشكوى الأصلية): مغلق → رسالة واضحة، مفتوح → يعبر الحارس، غير نقدي → دائماً مسموح. نُظّفت بيانات الاختبار بعد الإثبات.
> **المعادلة النهائية**: السبب الحقيقي للرسالة حُدِّد، والمسار الصحيح أُصلح دون تعطيل الحماية المحاسبية ودون إنشاء نسخة جديدة من التطبيق.
> **Release 1.3.5 — «العقل المحاسبي المحلي» لباتمان + إلغاء الضريبة (قرار الإدارة):**
> **العقل المحاسبي**: مجلد `knowledge/` — 8 وثائق محاسبية عربية (مصري) (المصطلحات، تكلفة الطعام، إجمالي الربح/نقطة التعادل، التكلفة الأولية، تصنيف المصروفات وقواعد الوردية النقدية، تقييم المخزون، دورة المشتريات، التقارير اليومية/الإقفال) + `importer.js` مبسّط ذاتي-التثبيت متكافئ (bootstrap بأعلى الصفحة، تجاهل الأخطاء، `LuccaDB.KnowledgeBase` في الوضعين) — والنتيجة: **12/12 اختبار وحدة معرفة** (التثبيت، التطابق، RAG عربي، تحميل الوثائق، التحديث عبر الـ tag، reset، أعلى الصفحة، التوافق القديم، تعطيل بلا ضرر، **الـ fallback المحلي عند غياب الجداول البعيدة**) بلا أي اتصال شبكي — ثم في الوضع المتصل لأول مرة **PGRST205** (جداول المعرفة غير منشأة في Supabase) → طُبِّق **fallback محلي تلقائي** (`lucca_knowledge_local` مستقلة) فتثبّتت المعرفة وعملت RAG حتى بلا جداول بعيدة — ولا يعود الاسترجاع في أي وضع سوى من الكيانات.
> **اختبارات حية على EXE مبني (12/12)**: A السكربتات كلها موجودة؛ B التثبيت (8 وثائق، 8 مواضيع) وجدول بعيد غائب (docsPersist=-1) فيطابق الوضع المحلي؛ C إعادة التثبيت لا تكرر؛ D استرجاع RAG عربي (تكلفة الطعام + التكلفة الأولية)؛ D2 الاستعلام بلا كلمات جوهرية لا يعيد شيئاً؛ E الأدوات المحاسبية على قاعدة بيانات حية (مبيعات، يومي، الصندوق، المخزون)؛ E2 تصدير CSV (توقيع BOM + تنسيق الأرقام، الإجمالي في `data.csv`)؛ F/F2 موزّع `accountingCommand` يوجّه بالاسم ويُرجع null للمجهول؛ G باتمان يحسب نسبة تكلفة الطعام بأرقام حية (صافي الإيرادات 689.7 — تخمير من قاعدة Supabase المتصلة)؛ G2 لا ادعاء نجاح وهمي؛ G3 إجابة معرفية خالصة عن دورة المشتريات مع مصادر.
> **إلغاء الضريبة («شيل الضريبة») — قرار الإدارة**: أُلغي احتساب الضريبة نهائياً: `admin/database.js` (إنشاء/تحديث الطلب) `order.tax=0`, `order.total=afterDiscount`؛ `index.html` `_autoSaveCore` يبني `tax: 0` + الإجمالي بعد الخصم فقط؛ حقل إعدادات الضريبة أصبح «الضريبة (ملغاة — 0%)» معطّلاً بقيمة 0 ويُحفظ 0 دائماً؛ سطر الضريبة في المعاينة/الطباعة مشروط بـ `tax > 0` فلن يظهر. والأهم أخيراً: `orderTotalsForDisplay` (المصدر الموحّد للمعاينة/الطباعة/الاسترداد) يصفّر الضريبة دائماً **حتى للطلبات القديمة** المحفوظة بسجل ضريبة سابق. **تحقق حي** (فحص TAX_PROBE على البناء الأخير): الحقل معطّل وقيمته 0، تسمية «ملغاة» ظاهرة، لا أثر لـ `|| 14`، **طلب قديم `tax=14` تُعرض فاتورته بلا سطر ضريبة (tax=0 والإجمالي التاريخي كما هو)**، والطلب الجديد بمخصوم 10% إجماليه = afterDiscount تماماً.
> **إلغاء شرط الوردية النقدية («فتح الشيفت») — قرار الإدارة (بناء L: سياسة جديدة)**: الكاش يعمل مباشرة في كل الحالات، لا شرط لفتح وردية/صندوق:
> • حُذف حجب الدفع النقدي من `selectPaymentAndClose` (index.html) — اختفى «لا توجد وردية نقدية… افتحها من زر 💰».
> • `saveExpenseFromText` (ai-pos-engine) يسجّل المصروف النقدي فوراً؛ وإن كانت وردية مفتوحة يربطه بها تلقائياً للتدقيق (best-effort) — زال كود `shift_required` وفرعه في batman-dashboard.
> • زر 💰 الصندوق ونظريته بقي اختيارياً للتسوية/الإقفال فقط (لا يحجب شيئاً)؛ رسائل باتمان/الواجهة أصبحت إرشادية.
> • وثائق المعرفة رُفعت إلى **1.0.1** (المصطلحات، تصنيف المصروفات، دورة المشتريات، إقفال اليوم) ليطابق المحتوى السياسة الجديدة (النقدي يُسجَّل مباشرة ويُربَط بالوردية إن كانت مفتوحة).
> **الاختبارات بعد السياسة الجديدة**: 29/29 وحدة + **12/12 حي** على البناء الجديد؛ فحص POLICY_PROBE حي: صفرية الضريبة حتى للطلبات القديمة (oldOrderTax=0، noVatRow)، ولا حجب (لا `shift_required`، لا رسالة فتح الشيف)، والمعرفة حاملة السياسة الجديدة؛ وفحص وظيفي حي **CASH_EXPENSE_NO_SHIFT**: وردية مغلقة → مصروف كاش (7) سُجِّل فعلياً في DB ثم نُظّف — دون أي طلب فتح وردية.
> **الحالة**: 29/29 وحدة (12 معرفة + 17 أدوات) + **12/12 حي** على 1.3.5؛ `latest.yml` يطابق الـ exe فعلياً؛ تم حذف آثار 1.3.4 من `dist`.

---

## بناء صيانة 1.3.5 (جلسة 2026-09-08) — إضافات وإصلاحات سابقة للاستخدام اليومي

1. **إضافة موظف تعمل أوفلاين**: كانت الإضافة بالبريد ترمي «تعذر الاتصال بالخادم» وتفشل محلياً في الوضع المنفصل → `Employees.invite()` (admin/database.js) تتنازل للإضافة المحلية عند انقطاع السيرفر (علامة `local`)، و`addEmployee`/`inviteEmployeeForm` (admin/index.html) تعرض النتيجة الصحيحة وتُفرّغ الحقول دائماً.
2. **كرت الشيفت في الشريط العلوي** (Quick-Strip باللوحة): `▶ ابدأ` / `⚡ تلقائي` — **صباحي 08:00–17:00 / مسائي 17:00–01:00** حسب الساعة — / `⏹ إنهاء`، مع عرض حالة الشيفت النشط لحظياً. `Shifts.start()` تحفظ الآن `shiftType`/`label`؛ وأزرار ورديّة باتمان (`start_shift`/`end_shift`/`shift_auto`) كانت بلا معالج → وُصلت بنفس المنطق.
3. **حذف ملفات ميتة من الحزمة**: `admin/_body.html`, `admin/_script.html`, `admin/index_part1.html` (نسخ قديمة مقسّمة غير مرجعية — `main.js` يحمّل `admin/index.html` فقط).
4. **اختبارات جديدة** (fake-indexeddb، بلا شبكة): `scripts/test-employee-invite.cjs` (الاحتياط المحلي للإضافة) + `scripts/test-shifts.cjs` (تصنيف الأوقات، start/end، منع التكرار) — **PASSED**، مع بقاء `test-admin-analytics.cjs` **32/32**.
5. **البناء**: portable `dist\LuccaPOS-win32-x64\LuccaPOS.exe` (build.bat) + إعادة بناء المثبّت `dist\LuccaPOS-Setup-1.3.5.exe` و`latest.yml` (electron-builder) — تحقّق آلي: إصلاحات الشيفت/الموظف متواجدة داخل الـ asar، وsha512 في `latest.yml` مطابق للـ exe فعلياً.

---

## إصلاح أونلاين/Supabase (2026-09-08) — `Employees.invite is not a function`

**التشخيص:** `admin/index.html` يحمّل `../supabase-db.js` (سطر 10) بعد `database.js`؛ محوّل Supabase يستبدل `window.LuccaDB` بكائن `Employees` ناقص (getAll/getActive/add/update/delete فقط — بلا `invite`/`setStatus`/`getInviteStatus`)، فينكسر إضافة الموظف في الوضع المتصل.

**الإصلاح في `supabase-db.js` — `Employees`:**
- `invite(payload)`: تجرّب `ServerAPI.post('/api/employees/invite')` أولاً؛ عند نجاحها تسجّل الدعوة في متجر `invitations` المحلي (الحالة) وترجع الرد. عند تعذّر الخادم تُنشئ الموظف مباشرة في `employees` وترجع `{local:true, name, id}`.
- `setStatus(id, active)`: تحدث النشاط عبر `/api/employees/:id/status` ثم put محلي.
- `getInviteStatus(employeeId)`: تقرأ متجر `invitations` المحلي وتُرجع used/expired/pending.

**`Shifts`:** أُضيفت `getToday`/`getActive`/`getByEmployeeAndDate`/`getByDateRange`/`getByEmployee`؛ و`start(employeeId, notes, {shiftType, label})` تخزّن النوع/الوصف؛ و`end` يحسب الساعات — بنفس واجهة الطبقة المحلية على هيئة camelCase.

**`quickLoadShiftStatus`** (admin/index.html) أصبح يقرأ الحقلين الدفاعيين camel/legacy (`employeeId ?? employee_id` و`startTime ?? start_time`).

**اختبار جديد:** `scripts/test-supabase-adapter.cjs` (محاكاة عميل Supabase بالذاكرة) — **PASSED**، وكل الاختبارات خضراء: analytics 32/32 + employee + shifts.

**إعادة البناء:** portable `dist\LuccaPOS-win32-x64\LuccaPOS.exe` (build.bat) + المثبّت `dist\LuccaPOS-Setup-1.3.5.exe` + `latest.yml` (electron-builder) — sha512 في `latest.yml` **متطابق فعلياً** (MATCH: true)، والفُحص داخل `app.asar` وجود `invite(payload)`/`setStatus`/`getInviteStatus`/`shiftType: options.shiftType`. حجم المثبّت: 108450969 بايت.
