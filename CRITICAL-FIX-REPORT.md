# تقرير الإصلاحات الحرجة (CRITICAL FIX REPORT)

**التاريخ:** 2026-08-31
**المرحلة:** CRITICAL (C1–C4)
**الحالة:** مكتملة ومتحقَّق منها

## نظرة عامة
تمت معالجة جميع الإصلاحات الحرجة الأربعة (C1–C4) المكتشفة في `PROJECT-AUDIT.md`.
جميع التعديلات تمت في `index.html` (الواجهة + محرك الفوترة + الطباعة).

---

## C1 — إصلاح خطأ Temporal Dead Zone في `openInvoiceModal`

### المشكلة
في الوظيفة `openInvoiceModal()`، كانت المتغيرات تُستدعى (تُقرأ) قبل تعريفها بـ `const`
داخل كتلة `try/catch { ... }`. هذا يؤدي إلى رمي `ReferenceError` يُبتلع بصمت بواسطة
`catch`، مما يمنع تشغيل تسجيل شاشة الولاء وتسجيل صندوق النقد (loyalty + cash-register)
عند فتح نافذة الفوترة.

### الحل
أعيدت كتابة الجزء العلوي من الدالة بحيث:
1. تُعرَّف جميع المتغيرات قبل استخدامها.
2. يُجلب الطلب المعلّق (pending order) أولاً.
3. تُستخدم القيم المخزنة في `total` (شامل الضريبة) لكل من شاشة الولاء وصندوق النقد.

---

## C4 — توحيد حسابات الإجمالي شاملة الضريبة (Tax-Inclusive)

### المشكلة
كانت `_autoSaveCore` تحفظ `total = afterDiscount + tax` أي إجمالي **شامل** الضريبة،
بينما كانت حسابات أخرى في الفوترة/الطباعة تعتمد على الإجمالي **قبل** الضريبة، مما
يؤدي إلى فوترة غير دقيقة عند العرض والطباعة ورسائل WhatsApp.

### الحل
أُضيفت دالة مساعدة موحدة:

```js
function orderTotalsForDisplay(order, fallbackSubtotal) { ... }
```

تُرجع الإجماليات الصحيحة (شاملة الضريبة) من الطلب. وطُبّقت هذه الدالة في:
- `generateWhatsAppMessage`
- `generateInvoiceHTML`
- `thermalPrintInvoice`
- نافذة الفوترة `openInvoiceModal`

---

## الملفات المعدّلة
- `index.html`
  - إضافة `orderTotalsForDisplay` (السطر ~548)
  - إعادة كتابة `openInvoiceModal` (السطر ~3090)
  - تحديث توليد الفاتورة ورسالة WhatsApp والطباعة الحرارية

## التحقق
- `node --check` على السكربت المضمّن في `index.html`: **ناجح (SYNTAX OK)**.
- كل الاستدعاءات المؤكّدة لـ `orderTotalsForDisplay` في 9 مواضع موجودة ومعنية به.

## ملاحظات / مخاطر متبقية (مؤجّلة إلى HIGH)
- الحالة الضريبية الخاصة بسطر الطباعة الحراري (thermal block) داخل كتلة C4 لم
  تُعاد مراجعتها بعد إعادة هيكلة `orderTotalsForDisplay` — تُؤجَّل للتحقق في مرحلة HIGH.

---
