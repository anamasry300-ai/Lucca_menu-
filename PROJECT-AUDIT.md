# PROJECT-AUDIT.md — فحص شامل لنظام Lucca POS

> **التاريخ:** 2026-08-31
> **الطريقة:** فحص قراءة فقط على الملفات المصدرية (لم يتم تعديل أي ملف).
> **الإصدار المفحوص:** 1.2.1

---

## 1) Architecture (البنية المعمارية)

**نوع التطبيق:** تطبيق سطح مكتب (Desktop) مبني بـ **Electron**، يعمل محليًا على الجهاز.

```
┌─────────────────────────────────────────────────────────────┐
│  Electron Main Process  (main.js)                           │
│   • إنشاء النافذة، القوائم، التحديث الذاتي، فتح admin        │
├─────────────────────────────────────────────────────────────┤
│  Renderer — التطبيق الرئيسي  (index.html — 6630 سطر، ~389KB)│
│   • POS: الطاولات/الطلبات/الفاتورة/المحاسبة    (نص مدمج)     │
│   • المحاسب الذكي "باتمان" + استدعاء Ollama محليًا           │
├─────────────────────────────────────────────────────────────┤
│  Renderer — لوحة التحكم  (admin/index.html + database.js)   │
│   • قاعدة البيانات المحلية IndexedDB «lucca_caffe_db» نسخة 8 │
│   • إدارة المنتجات/الموظفين/المخزون/التقارير... (33 مخزن)    │
├─────────────────────────────────────────────────────────────┤
│  قاعدة البيانات المحلية: IndexedDB (في المتصفح/Electron)    │
│  السيرفر الاختياري: backend/ (Express + sql.js) → port 3000 │
│   • غير مشغَّل من التطبيق، منفصل تمامًا، لا يدخل البناء      │
├─────────────────────────────────────────────────────────────┤
│  ملفات مساندة: ai-pos-engine.js, supabase-db.js,            │
│  sync-engine.js, report-export.js, forecasting.js,          │
│  ollama-adapter.js, knowledge-base.js, menu-data.js          │
└─────────────────────────────────────────────────────────────┘
```

### الطبقات
| الطبقة | الملف | الوصف |
|---|---|---|
| الواجهة الرئيسية | `index.html` | تطبيق POS كامل (جدوى كل العمليات) بنص برمجي واحد ضخم |
| لوحة التحكم | `admin/index.html`, `admin/database.js` | إدارة + قاعدة بيانات IndexedDB + API منفذ |
| المعالج الرئيسي | `main.js` + `preload.js` | دورة حياة Electron، IPC، التحديث الذاتي |
| محرك الأوامر | `ai-pos-engine.js` | محرك أوامر عربي (الشات بوت) |
| قاعدة البيانات | IndexedDB «lucca_caffe_db» v8 | 33 مخزن (users, tables, orders, products, ...) |
| سيرفر اختياري | `backend/` (Express+sql.js) | REST API محلي على منفذ 3000 |
| الذكاء الاصطناعي | `ollama-adapter.js` + Ollama | محاسب "باتمان" محلي عبر qwen2.5-coder |
| تصدير | `report-export.js` | تصدير Excel عبر SheetJS (من CDN) |
| التزامن | `sync-engine.js`, `supabase-db.js` | مزامنة اختيارية (Supabase معطّل عمليًا) |

### الأسئلة المحورية
- **الـ Frontend:** `index.html` (POS) + `admin/index.html` (لوحة تحكم) + `kitchen.html` (شاشة المطبخ).
- **الـ Backend:** يوجد `backend/` حقيقي (Express + sql.js / SQLite-في-الذاكرة). **لكنه لا يُشغَّل من التطبيق ولا يدخل البناء** → في الاستخدام الفعلي النظام محلي بالكامل.
- **قاعدة البيانات:** IndexedDB محلية داخل الجهاز (33 مخزن). السيرفر مزوّد إن شُغِّل يدويًا بشكل منفصل.

---

## 2) طريقة تشغيل البرنامج حاليًا

- **التشغيل التطويري:** `npx electron .` أو `start.bat` (يفحص Node + Electron ثم يشغّل `npx electron .`).
- **البناء/التوزيع:** `build.bat` أو `npm run build` → `electron-packager` ينتج `dist/LuccaPOS-win32-x64/` (نسخة غير قابلة للتثبيت portable، بدون asar في بعض الحالات). توجد أيضًا `build:installer` عبر electron-builder→NSIS.
- **التوزيع / التحديث:** نظام **تحديث ذاتي** في `main.js` يسحب ملفات فردية من GitHub raw (`UPDATE_FILES`) — **لا يستخدم electron-updater رغم كونه في dependencies**.
- **backends:** لا يُشغَّل `backend/` تلقائيًا؛ التطبيق يعمل بدون سيرفر.

---

## 3) الملفات المسؤولة عن كل مجال

| المجال | الملف/الوظيفة |
|---|---|
| **المبيعات** | `admin/database.js → Orders.checkout()`, `Orders.getDailySales()`, `index.html → openInvoiceModal()` |
| **الفواتير** | `admin/database.js → Orders.checkout()` (ينشئ مخزن `invoices`), `index.html → generateInvoiceHTML, thermalPrintInvoice`, `kitchen.html` |
| **المنتجات والأصناف** | `admin/database.js → Products`, `menu-data.js`, `admin/index.html` (إدارة) |
| **التصنيفات** | `admin/database.js → Categories`, `index.html` (تبويبات المنيو) |
| **الطاولات والطلبات** | `admin/database.js → Tables, Orders`; `index.html → loadTables, freeSelectedTable, quickAction, openInvoiceModal` |
| **طرق الدفع** | `admin/database.js → PaymentMethods`, `index.html → selectPaymentAndClose` (كاش/فيزا/...) |
| **المشتريات** | `admin/database.js → Purchases` (يضيف تلقائيًا للمخزون + `Inventory.adjustStock`) |
| **المخزون** | `admin/database.js → Inventory, StockMovements, WasteLog, ProductRecipes` |
| **المصروفات** | `admin/database.js → Expenses`; `index.html` (تسجيل مصروف) |
| **الموظفين والحضور** | `admin/database.js → Employees, Attendance, Shifts` |
| **التقارير** | `backend/src/routes/analytics.ts`, `report-export.js`, `admin/index.html` (Dashboard/تقارير) |
| **المستخدمين والصلاحيات** | `admin/database.js → Users`, `admin/index.html → doLogin`, `supabase-db.js` |

---

## 4) الأخطاء والأجزاء غير المكتملة (ملخص)

### 🔴 CRITICAL

| # | المشكلة | الموقع | التفصيل |
|---|---|---|---|
| C1 | **فقدان إيراد / أعطال المحاسبة بسبب Temporal Dead Zone (TDZ)** | `index.html:3081` و `3092` | داخل `openInvoiceModal()`: يُقرأ `order` (سطر 3081) و `formData` (3081) و `total` (3092) **قبل** إعلانها `const` (الأعمار 3101/3103/3112) → `ReferenceError` في كل استدعاء → تُبلع بواسطة `try/catch`. النتيجة: **نقاط الولاء لا تُضاف أبدًا**، و**سجل الصندوق (CashDrawer) لا يسجّل بعمليات البيع** — أي أن صندوق الكاش لا يعكس المبيعات. |
| C2 | **واجهة/سيرفر لا يلتزان دائمًا + خطر مسح البيانات** | `backend/src/index.ts:200 (/api/sync)` + client | `/api/sync` يمسح الجداول (`DELETE FROM` ثم أعادة إدراج) بدون كشف تعارض. أي عميل محلي بأحدث بيانات قديمة/فارغة **يمسح بيانات الخادم**. لا يوجد last-write-wins آمن ولا دمج. |
| C3 | **النسخ الاحتياطي ناقص → فقدان بيانات دائم** | `admin/database.js → DataSync.exportAll/importAll` | لا تُصدَّر مخازن: **`invoices`**, `order_items`, `suppliers`, `stock_movements`, `product_recipes`, `waste_log`, `knowledge_*`, `botMemory`. الاسترجاع بعد نسخة احتياطية **يفقد سجل الفواتير الثابت (immutable) وبند الطلبات (order_items)** → تحليل التكلفة والمبيعات بالمنتج ينهار، وسجل المحاسبة غير مكتمل. |
| C4 | **إجمالي الفاتورة المعروض ≠ المُحصَّل (الضريبة تُسقط)** | `index.html:3112` | في `openInvoiceModal` يُحسب `total = subtotal - (subtotal*discount/100)` **بدون إضافة الضريبة (tax)**، بينما `Orders.checkout` يُدرج الضريبة في المبلغ المحصَّل. → الفاتورة المطبوعة تعرض مبلغًا أقل من المدفوع. |

### 🟠 HIGH

| # | المشكلة | الموقع | التفصيل |
|---|---|---|---|
| H1 | **مفتاح API مكشوف عمومًا + لا مصادقة حقيقية** | `backend/src/index.ts:95 /api/public-key` و `apiKeyCheck` | `GET /api/public-key` يسلّم المفتاح للجميع. أي طلب يحمل `x-api-key` أو `Bearer` بالمفتاح الثابت `lucca-secret-key` يحصل على وصول كامل. لا JWT، لا جلسات على السيرفر. |
| H2 | **الصلاحيات في الواجهة فقط (غير آمنة)** | `admin/index.html:doLogin`, `Users.login` | `currentUser` + الدور مخزّنان في `localStorage` (قابل للتزوير من قِبل المستخدم). كلمة المرور الافتراضية للمدير `123456` (createDefaultAdmin). كل الحماية عميلية. |
| H3 | **معالجة الدفع غير ذرّية → فقدان جزئي عند انقطاع/تعطل** | `admin/database.js:801-882 (checkout)` | عدة `add/put` منفصلة بدون معاملة واحدة (invoice → payment → inventory → order → table). إن تعطل التطبيق في المنتصف، يبقى النظام في حالة وسيطة (فاتورة بلا إغلاق، أو طاولة تحررت قبل حفظ الطلب). |
| H4 | **البيانات محلية فقط في الاستخدام الفعلي → لا تعدد أجهزة** | `main.js` (لا spawn للـ backend) + build يهمل `backend/` | السيرفر غير مضمّن ولا يُشغَّل؛ فكل جهاز لديه IndexedDB منفصل. أي جهاز ثانٍ لا يرى بيانات الجهاز الأول. نظام المزامنة الحقيقي غير مُفعَّل عمليًا. |
| H5 | **`getDailySales` يحسب طلبات `pending` كجزء من مبيعات اليوم** | `admin/database.js:925-929` | يشمل `status 'pending'` في مبيعات اليوم → **مبالغة/عد مزدوج محتمل** للإيرادات (طلبات قيد التحضير غير المدفوعة تُحتسب كإيراد). |
| H6 | **تصدير Excel يعتمد على CDN → يفشل بدون إنترنت** | `report-export.js:8` | SheetJS يُحمَّل من `cdn.sheetjs.com` على الطلب. POS يعمل غالبًا على جهاز/شبكة دون إنترنت → **زر تصدير Excel لا يعمل أبدًا بدون اتصال**. |

### 🟡 MEDIUM

| # | المشكلة | الموقع | التفصيل |
|---|---|---|---|
| M1 | **تأخير الشات حتى 90 ثانية (Ollama)** | `index.html:6562` | fallback Ollama بـ `timeout:90000` يحجب رد الشات؛ مع بطء qwen على هذا الجهاز قد يُحس المستخدم تعطلاً قبل هبوط الرد الافتراضي. |
| M2 | **العربية/الترميز في وحدات التحكم** | `main.js`, `start.bat` | أسماء ملفات/مخرجات بترميز مغلوط في Console (لا يؤثر على التطبيق نفسه). |
| M3 | **ملف HTML ضخم بـ 6630 سطر / 389KB** | `index.html` | صعوبة الصيانة، بطء تحميل/تحليل، تضارب نطاقات. |
| M4 | **مصادر خارجية (CDN) رغم أنه POS محلي** | `index.html:10-12` | Google Fonts, qrcodejs, supabase-js من CDN. رغم أن supabase متعطّل، يبقى تبعية إنترنت. |
| M5 | **`order`/`formData`/`total` مُعاد استخدامها بأسماء عامة** | `index.html` | عدة وظائف تعيد تعريف نفس الأسماء — خطأ تجنب سهل، ويسبب مع TDZ أعلاه. |

### 🟢 LOW

| # | المشكلة | الموقع |
|---|---|---|
| L1 | الإصدار مكرر في 3 أماكن (package.json, version.json, وحقول Git) بلا مصدر واحد | `package.json:3`, `version.json:2` |
| L2 | `execute()`/`insert()` في backend يرجّع `{changes:0,lastInsertRowid:0}` دائمًا؛ استدعاءات تعتمد على `getLastInsertId` غير دقيقة | `backend/src/db.ts:62-90` |
| L3 | `DataSync.importAll` يمسح كل مخزن قبل الاستيراد دون تأكيد/تحقق | `admin/database.js:2216` |
| L4 | `README.md` غير محدّث (محتوى تجريبي) | `README.md` |
| L5 | `deploy/` و `backend/` نسخ متعددة متشعبة (supabase-db.js مكرر) | `deploy/`, `supabase-db.js` |

---

## 5) فحص مشكلة الطاولة والطلب (التدفق الكامل)

**التدفق الطبيعي (مُحدَّد من الكود):**

1. **اختيار الطاولة** → `loadTables()` / `loadTablesMainView()` يعرض الطاولات بأحوالها (`available/occupied/reserved/cleaning/closed`)؛ اختيار طاولة مشغولة يفتح طلبها الحالي.
2. **إضافة الطلب** → إضافة أصناف لقائمة `currentOrder` (في `index.html`).
3. **حفظ الطلب** → `LuccaDB.Orders.add/update` (يخزن في IndexedDB، مع `status: pending`)، وتُحدَّث الطاولة إلى `occupied` وربطها بـ `currentOrder`.
4. **طلب الشيك / الحساب** → `selectPaymentAndClose()` → `openInvoiceModal`.
5. **إصدار الفاتورة** → `LuccaDB.Orders.checkout(orderId, paymentMethod)` يُنشئ `invoice` + `payment` + يخصم المخزون + يُغلق الطلب + يحرّر الطاولة، ثم يعرض الفاتورة (طباعة حرارية/عادية/واتساب).
6. **إغلاق الطلب / الدفع** → `Orders.checkout` يضبط `status='closed'`, `paymentStatus='paid'`.
7. **تحرير الطاولة** → يُحرَّر تلقائيًا داخل `checkout` (يجعل `status='available', currentOrder=null`). وأيضًا `freeSelectedTable()` (ينهي الطلب `cancelled` ثم يحرّر الطاولة يدويًا).

**النتيجة وملاحظات المخاطر:**
- ✔ التدفق موجود ومتماسك وظاهريًا يعمل.
- ⚠ **`freeSelectedTable` ينهي الطلب `cancelled`** بدل إغلاقه محاسبيًا — إن استُخدم لتحرير طاولة عليها طلب غير مدفوع، **يُفقد الإيراد دون فاتورة**. يجب التمييز بين "تحرير طاولة" (للطاولات المحجوزة/الفارغة) و"إغلاق حساب".
- ⚠ **الدفع غير ذرّي** (انظر C/H) → خطر حالة وسيطة عند التعطل.
- ⚠ **عيوب TDZ** تجعل ولاء/الصندوق معطّلين (C1).
- ⚠ **تفاوت الضريبة بين الفاتورة والمدفوع** (C4).
- ⚠ عدّ `pending` ضمن مبيعات اليوم (H5).

---

## 6) نظام المحاسبة وسلامة البيانات

- **الدفع/الإيراد:** يُسجَّل عبر `Orders.checkout` → `invoices` + `payments` (مخازن منفصلة). 
- **الصندوق:** `CashRegister` موجود لكن تسجيل عمليات البيع معطّل عمليًا (C1 → سطر 3092).
- **الضريبة:** موجودة (إعداد `taxRate` افتراضي 14) لكن إسقاطها في عرض الفاتورة (C4) يخلق تفاوتًا.
- **النسخ الاحتياطي:** `DataSync.exportAll/importAll` ناقص (لا يشمل `invoices`/`order_items`) → **C3: لا يمكن استرجاع السجل المحاسبي الكامل بعد النسخ/الاسترجاع**.
- **الذّرّة/الاسترداد:** كتابات غير ذرّية (H3)؛ استردادات (refunds) موجودة لكن غير موحّدة مع فتح الطاولة/المخزون.
- **العمليات المتعددة الأجهزة:** لا تُدَعَّم فعليًا (H4).
- **الخلاصة:** المحاسبة أحادية الجهاز تعمل في المسار الوحيد الأبسط، لكن **عرضة لفقدان جزئي أو فقدان بيانات عند النسخ الاحتياطي/التحديث/التعطل**، وتركت ثغرات فعّالة (الولاء والصندوق).

---

## 7) Dashboard والتقارير

- **الإحصاءات:** Dashboard في `admin/index.html` يعرض مؤشرات (مبيعات اليوم، مصروفات، طلبات، متوسط طلب...) تُستنتج من IndexedDB.
- **الإيرادات:** تعتمد على تصفية الطلبات حسب التاريخ و`status` — ويشمل ذلك `pending` (H5).
- **التقارير:** `backend/src/routes/analytics.ts` يوفر `/api/reports/summary`, `sales-by-category`, `daily-sales`... لكنه يتطلب السيرفر الذي لا يعمل أساسًا. **عند غيابه، لا توجد واجهة تقارير محلية قوية داخل admin تعادل تلك** (تعتمد على سيرفر غير مشغَّل).
- **خلاصة:** Dashboard الأساسي يعمل محليًا، لكن التقارير التحليلية المتقدمة مربوطة بسيرفر غير نشط → غير متاحة عمليًا.

---

## 8) تصدير التقارير إلى Excel

- **الوظيفة موجودة:** `report-export.js` (SheetJS/XLSX) يولّد مصنفًا متعدد الأوراق (مبيعات/منتجات/مصروفات/موظفين/مخزون/تكاليف/مدفوعات) عبر `ReportExport.generateFullReport()` و `exportSheet()`.
- **العقبة الحرجة:** SheetJS يُحمَّل من **CDN** (`cdn.sheetjs.com`) → **لا يعمل بدون إنترنت** (H6). POS محلي/مكسوف غالبًا لا يملك إنترنت.
- **ملاحظة:** الأعمدة تُعرض بالعربية بشكل صحيح؛ الأرقام قد تُخرج كنصوص حسب كولوم «الإجمالي» (Number) — جيد.

---

## 9) الأداء وسرعة التشغيل

- **index.html ضخم (389KB نص برمجي مدمج)** — بطء أولي في التحليل، وجهود تصحيح صعبة.
- **استدعاءات Ollama** تصل إلى 90 ثانية → تأثير على استجابة الشات (M1).
- **IndexedDB**: المزامنة الدورية عبر التجوال لكل صف (cursor) بدون فهارس/تجزئة → بطء مع بيانات كثيرة.
- **البناء/التشغيل:** electron-packager خفيف، لكن لا يوجد كسر/تقسيم للكود، ولا lazy-load للمكونات.
- **الخلاصة:** أداء مقبول على جهاز واحد ببيانات قليلة، لكنه سيتدهور مع مرور الوقت وكثرة السجلات.

---

## 10) التكامل مع Ollama و AI Reviewer

- **Ollama المحلي (محاسب "باتمان"):** 
  - `ollama-adapter.js` مدمج في `index.html` (سطر 15).
  - `buildLiveContext()` (سطر 6593) يجمع بيانات حية (المبيعات/المصروفات/الطاولات) ثم `OllamaAI.chat()` (سطر 6562) يرد بالعربية.
  - **الأمر يعمل** (تحققنا سابقًا: ردَّ بالأرقام الصحيحة).
- **النموذج:** `qwen2.5-coder:7b` يعمل محليًا لكنه بطيء (30–240 ثانية لكل استدعاء).
- **AI Reviewer:** `ai-reviewer.js` (سكربت Node مستقل) يفشل لأنه لا ينتج `tool_calls` مهيكل — **غير صالح كعامل داخل OpenCode**، لكنه يصلح كمحلل عبر نفَس الملفات (نفّذناه سابقًا بنجاح مع حدود مهلة أطول).
- **نقاط الضعف:**
  - تأخر رد الشات حتى 90 ثانية (M1).
  - لا تحقق من النتائج المحاسبية قبل الرد — يعتمد على `buildLiveContext` الذي **يشمل `pending`** ويعتمد على `status` المتنوع (يستحق توحيد معطياته).
  - `ai-reviewer.js` غير مُضمَّن في التوزيع/البناء.

---

## 11) خطة الأولويات المقترحة (لا تُنفَّذ الآن — بانتظار موافقتك)

### المرحلة 1 — سدّ الثغرات الحرجة (CRITICAL)
1. **إصلاح TDZ في `openInvoiceModal`** (C1): إعادة ترتيب الإعلانات أو تمرير المتغيرات بشكل سليم → يعيد تفعيل نقاط الولاء وتسجيل الصندوق للمبيعات.
2. **تصحيح حساب الفاتورة ليشمل الضريبة** (C4) → تطابق المبلغ المعروض مع المحصَّل.
3. **إكمال `DataSync.exportAll/importAll`** بكل المخازن (خصوصًا `invoices`, `order_items`) (C3) + تعطيل المسح العشوائي في الاستيراد.
4. **حماية `/api/sync`** من مسح بيانات الخادم بنسخة فارغة/قديمة (C2): إضافة تحقق بعدد السجلات/طابع زمني، أو مزامنة دلتا بدل مسح كامل.

### المرحلة 2 — الأمان (HIGH)
5. **تعطيل `GET /api/public-key`** وإدخال مصادقة حقيقية (JWT/جلسات) للمستخدمين بدل مفتاح ثابت مكشوف (H1).
6. **نقل الصلاحيات وضبطها خادميًا** + فرض تغيير كلمة مرور `admin/123456` الافتراضية، وتعميم التجزئة (PBKDF2) لجميع الحسابات (H2).
7. **جعل معالجة الدفع ذرّية** (معاملة IndexedDB واحدة عبر المخازن المتأثرة) + استرداد/recovery عند إعادة التشغيل (H3).

### المرحلة 3 — التوزيع والتعددية (HIGH)
8. **تضمين backend مع التطبيق** (spawn تلقائي) أو الاعتماد النهائي على قاعدة المحلية لقاعدة بيانات فعالة محلية (SQLite) (H4).
9. **معالجة احتساب `pending`** في مبيعات اليوم/التقارير (H5).
10. **تضمين SheetJS محليًا** لتصدير Excel يعمل بدون إنترنت (H6).

### المرحلة 4 — الأداء والاستقرار (MEDIUM)
11. تقليل زمن استجابة Ollama (مهلة أقصر + تجميع ذكي) وإنهاء "باتمان" بعدد طلبات (M1).
12. تقسيم `index.html` الضخم إلى وحدات/ملفات JS (M3) وتوحيد أسماء المتغيرات (M5).

### المرحلة 5 — الحوكمة والتنظيف (LOW)
13. مصدر واحد للإصدار (L1)، إصلاح `execute/insert` في backend (L2)، تحديث README (L4)، إزالة التشعّب في deploy/supabase (L5).

---

## ملاحظة منهجية
> تم تنفيذ هذا الفحص بالقراءة المباشرة للملفات بعد أن تعطّلت أدوات الفحص الآلي الداخلية (خطأ `no such column: replacement_seq` في بيئة الأدوات، لا علاقة له بالمشروع). رجاءً اعتمد على هذه النتائج كنقاط مراجعة، وتأكد منها عند التنفيذ الفعلي لكل بند، فبعض النتائج (مثل تفاوت الفاتورة، وأثر TDZ) تستدعي تأكيدًا وقت التشغيل.
